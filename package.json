{
  "name": "json-server-api",
  "version": "1.0.0",
  "scripts": {
     "start": "json-server db.json --port ${PORT:-3000} --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
