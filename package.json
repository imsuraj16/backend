{
  "name": "json-server-api",
  "version": "1.0.0",
  "scripts": {
    "start": "npx json-server --watch db.json --port 4000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}